#include <iostream> 
#include <bits/stdc++.h>
#include <math.h>
using namespace std;
int main(){
    // largest word in a sentence
    int n;
    cin>>n;
    cin.ignore();
    char arr[n+1];

    cin.getline(arr,n);
    cin.ignore();
    int i=0;
    int currlen=0;
    int mxlen=0;
    while (1)
    {
        if(arr[i] ==' ' || arr[i]='\0'){
              if(currlen>maxlen){
                  
              }
        }
        if(arr[i]=='\0'){
            break;
        }

    }
    
}